<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/custom_toolbar_shopping"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@android:color/white"
    android:theme="@style/ThemeOverlay.AppCompat.Light"
    android:elevation="0dp">

    <ImageView
        android:id="@+id/toolbar_menu_shopping"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="8dp"
        android:src="@drawable/ic_menu"
        android:contentDescription="@string/menu_icon" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/toolbar_export_shopping"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_export_2"
            android:contentDescription="@string/export_icon"
            android:layout_marginEnd="16dp" />

        <ImageView
            android:id="@+id/toolbar_add_shopping"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_add"
            android:contentDescription="@string/add_icon"
            android:layout_marginEnd="12dp" />
    </LinearLayout>
</androidx.appcompat.widget.Toolbar>
